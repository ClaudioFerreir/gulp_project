* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

.modal-header {
  border-bottom: 1px solid #5b3a30;
}

header {
  background-color: #e5dbd0;
  position: sticky;
  top: 0;
  left: 0;
  box-shadow: 2px 2px 2px #5b3a30;
  z-index: 1;
}

.dropdown-menu,
.modal-content,
.btn-secondary,
#rodape {
  color: #5b3a30;
  background-color: #e5dbd0;
}

#navbar,
h1,
h4,
.dropdown-menu.show li a,
.nav-link.dropdown-toggle.show {
  color: #5b3a30;
}

.navbar nav .nav-link.active,
.dropdown-menu.show li a:hover,
.btn-secondary:hover {
  color: #e5dbd0;
  background-color: #5b3a30;
}

.dropdown-menu {
  border: none;
  filter: drop-shadow(2px 2px 2px #5b3a30);
  z-index: 1;
}

.nav-link {
  text-align: center;
  color: #5b3a30;
}

/*Carousel section*/
.carousel-item img {
  height: 80vh;
  object-fit: cover;
}

/*Lojas section*/
#lojas {
  height: auto;
  color: #e5dbd0;
  background-color: #5b3a30;
}

#lojas select {
  height: 3em;
}

/*Contato section*/
#contato {
  background-color: #5b3a30;
}

#contato p {
  color: #e5dbd0;
}

/*footer*/
.separated {
  width: 1px;
  background-color: #5b3a30;
  align-self: stretch;
}

#rodape a {
  color: #5b3a30;
}

@media screen and (max-width: 767px) {
  #aviso-modal {
    max-width: 50vh;
  }
  #carouselMain img {
    height: 50vh;
  }
  #lojas h3 {
    padding-bottom: 1em;
  }
  #produtos h4 {
    font-size: 2em;
  }
  #produtos a {
    width: 30vh;
  }
  .toast-container {
    max-width: 50vh;
    position: fixed;
    left: 0;
  }
  #contato > div,
  #rodape > div {
    flex-direction: column;
  }
  #contato strong {
    display: block;
    text-align: center;
  }
  .copy {
    border-top: 1px solid #5b3a30;
    font-size: 10px;
  }
  .copy2 {
    font-size: 10px;
  }
}

/*# sourceMappingURL=main.css.map */
